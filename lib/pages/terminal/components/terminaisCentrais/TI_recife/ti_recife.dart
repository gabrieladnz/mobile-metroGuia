import 'package:flutter/material.dart';

import 'body_recife.dart';

class Recife extends StatelessWidget {
  const Recife({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyRecife(),
    );
  }
}
