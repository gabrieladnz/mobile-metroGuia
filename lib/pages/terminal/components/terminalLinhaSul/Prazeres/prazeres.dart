import 'package:flutter/material.dart';
import 'body_prazeres.dart';

class Prazeres extends StatelessWidget {
  const Prazeres({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyPrazeres(),
    );
  }
}
