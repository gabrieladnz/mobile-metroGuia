import 'package:flutter/material.dart';

import 'body_tancredo.dart';

class TancredoNeves extends StatelessWidget {
  const TancredoNeves({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyTancredo(),
    );
  }
}
