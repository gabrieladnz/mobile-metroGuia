import 'package:flutter/material.dart';
import 'body_cajueiro_seco.dart';

class CajueiroSeco extends StatelessWidget {
  const CajueiroSeco({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyCajueiroSeco(),
    );
  }
}
