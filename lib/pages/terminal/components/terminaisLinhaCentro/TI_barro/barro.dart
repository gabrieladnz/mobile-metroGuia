import 'package:flutter/material.dart';
import 'body_barro.dart';

class Barro extends StatelessWidget {
  const Barro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyBarro(),
    );
  }
}
