import 'package:flutter/material.dart';
import 'body_tejipio.dart';

class Tejipio extends StatelessWidget {
  const Tejipio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyTejipio(),
    );
  }
}
