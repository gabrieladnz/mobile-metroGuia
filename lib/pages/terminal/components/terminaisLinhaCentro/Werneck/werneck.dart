import 'package:flutter/material.dart';

import 'body_werneck.dart';

class Werneck extends StatelessWidget {
  const Werneck({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyWerneck(),
    );
  }
}
