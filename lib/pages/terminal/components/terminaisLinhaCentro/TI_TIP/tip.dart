import 'package:flutter/material.dart';
import 'body_tip.dart';

class TIP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyTIP(),
    );
  }
}
