import 'package:flutter/material.dart';

import 'components/body_camaragibe.dart';

class Camaragibe extends StatelessWidget {
  const Camaragibe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: bodyCamaragibe(),
    );
  }
}
