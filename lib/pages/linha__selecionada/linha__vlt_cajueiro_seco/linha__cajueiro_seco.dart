import 'package:flutter/material.dart';
import 'components/body_cajueiro.dart';

class CajueiroVLT extends StatelessWidget {
  const CajueiroVLT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body: bodyCajueiro(),
    );
  }
}
